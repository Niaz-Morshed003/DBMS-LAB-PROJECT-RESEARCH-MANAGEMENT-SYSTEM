# Gmail OTP ভিত্তিক অ্যাকাউন্ট ভেরিফিকেশন ও রেজিস্ট্রেশন সিস্টেম প্ল্যান

বর্তমানে সিস্টেমে অ্যাকাউন্ট খোলার জন্য অ্যাডমিন নোটিফিকেশনের ওপর নির্ভর করতে হয় (অ্যাডমিন অ্যাপ্রুভ না করলে অ্যাকাউন্ট তৈরি হতো না)। এই আর্কিটেকচার পরিবর্তন করে প্রফেশনাল প্ল্যাটফর্মের মতো **Gmail Verification Code (OTP)** সিস্টেম যুক্ত করা হবে। ব্যবহারকারী সাইন-আপ করার সময় তার জিমেইলে একটি ৬-সংখ্যার ভেরিফিকেশন কোড পাঠানো হবে। কোডটি ভেরিফাই করলেই তাৎক্ষণিকভাবে অ্যাকাউন্ট তৈরি হয়ে যাবে।

অন্য কোনো অপ্রয়োজনীয় ফাইলে কোনো পরিবর্তন করা হবে না—শুধুমাত্র সাইন-আপ ও ভেরিফিকেশন সংশ্লিষ্ট অংশটুকুই নিখুঁতভাবে আপডেট করা হবে।

---

## কাজের বিস্তারিত পরিকল্পনা

### ১. জিমেইল কনফিগারেশন (Gmail SMTP Settings)
- পাইথনের স্ট্যান্ডার্ড লাইব্রেরি `smtplib` ও `email.mime` ব্যবহার করে জিমেইল পাঠানো হবে।
- প্রজেক্টে একটি `.env` ফাইল থাকবে যেখানে জিমেইল ইউজারনেম এবং গুগল অ্যাপ পাসওয়ার্ড (১৬ ডিজিট) থাকবে।
- টেস্টিং সুবিধার জন্য কোডটি একইসাথে ব্যাকএন্ডের টার্মিনাল/কনসোলেও প্রিন্ট হবে যাতে জিমেইল ক্রেডেনশিয়াল ছাড়া থাকলেও সাথে সাথে টেস্ট করা যায়।

### ২. ব্যাকএন্ড স্কিমা স্তর (`schemas.py`)
- `SendOTPRequest`: ইমেইল পাঠানোর জন্য স্কিমা (`email: EmailStr`)।
- `FacultyCreate` ও `AdminCreate`: ওটিপি ভেরিফাই করে একাউন্ট তৈরি করতে `otp` ফিল্ড যোগ করা।

### ৩. বিজনেস লজিক (`crud.py`)
- `send_verification_otp(db, email)`: ডুপ্লিকেট ইমেইল চেক করবে, ৬ সংখ্যার র‍্যান্ডম ওটিপি তৈরি করে জিমেইলে পাঠাবে এবং ৫ মিনিটের জন্য মেমোরিতে টাইমস্ট্যাম্প সহ রাখবে।
- `verify_otp(email, otp)`: ওটিপি সঠিক কিনা তা যাচাই করবে।
- সঠিক ওটিপি পাওয়া গেলে সরাসরি `create_faculty` বা `create_admin` দিয়ে মূল ডাটাবেজে একাউন্ট সেভ করবে।

### ৪. ব্যাকএন্ড এন্ডপয়েন্ট (`NiazBackend.py`)
- `POST /auth/send-otp`: ওটিপি জেনারেট ও পাঠানোর এপিআই।
- `POST /auth/signup/faculty` ও `POST /auth/signup/admin`: ওটিপি যাচাই করে সরাসরি একাউন্ট তৈরি করবে (অ্যাডমিন পেন্ডিং নোটিফিকেশন আর লাগবে না)।

### ৫. ফ্রন্টএন্ড ইন্টারফেস (`frontend.html`)
- সাইন-আপ ফর্মে সাবমিট বাটনে চাপলে স্বয়ংক্রিয়ভাবে ব্যাকএন্ড থেকে জিমেইলে ওটিপি যাবে।
- স্ক্রিনে একটি আকর্ষণীয় পপ-আপ মডাল (`Enter 6-digit Code`) আসবে।
- কোডটি দিয়ে কনফার্ম করলেই একাউন্ট তৈরি সম্পন্ন হবে এবং সরাসরি লগইন করতে পারবে।
