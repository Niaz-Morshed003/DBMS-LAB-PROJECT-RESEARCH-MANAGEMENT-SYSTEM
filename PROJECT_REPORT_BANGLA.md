# রিসার্চ ম্যানেজমেন্ট সিস্টেম — পূর্ণাঙ্গ প্রজেক্ট রিপোর্ট (DBMS Lab Project)

**প্রজেক্টের নাম:** Research Management Portal (Role-Based Academic Infrastructure)  
**ভার্সন:** ২.২.০  
**উদ্দেশ্য:** বিশ্ববিদ্যালয় পর্যায়ের (UIU ডোমেইন ভিত্তিক) ফ্যাকাল্টি, অ্যাডমিন এবং শিক্ষার্থীদের রিসার্চ প্রজেক্ট, আবেদন ও পরিসংখ্যান ব্যবস্থাপনার একটি কেন্দ্রীয় ওয়েব প্ল্যাটফর্ম।

---

## ১. প্রজেক্ট পরিচিতি ও আর্কিটেকচার ওভারভিউ

এই প্রজেক্টটি একটি রোল-বেসড একাডেমিক পোর্টাল। এটি ডাটাবেজ ম্যানেজমেন্ট সিস্টেম (DBMS) কোর্সের ল্যাব প্রজেক্ট হিসেবে তৈরি করা হয়েছে। এখানে রিলেশনাল ডাটাবেজের মূল নীতিসমূহ—যেমন ফরেন কি কনস্ট্রেইন্ট (`ON DELETE CASCADE`), কম্পোজিট কি (Composite Primary Keys), কাঁচা SQL কোয়েরি (`RAW SQL parameterized queries`), গ্রুপ বাই (`GROUP BY`), সাব-কোয়েরি (`Subqueries`), এবং ডাটাবেজ ভিউ (`SQL Views`) ব্যবহার করা হয়েছে।

### ব্যবহৃত টেকনোলজি স্ট্যাক:
* **ব্যাকএন্ড ফ্রেমওয়ার্ক:** Python 3 + FastAPI
* **ডাটাবেজ ও কানেক্টর:** MySQL (InnoDB Engine) + PyMySQL
* **ওআরএম/ডাটা এক্সেস লেয়ার:** SQLAlchemy (Core & ORM Session)
* **ডাটা ভ্যালিডেশন:** Pydantic (`BaseModel`, `EmailStr`)
* **ফ্রন্টএন্ড আর্কিটেকচার:** পিওর সিঙ্গেল পেজ অ্যাপ্লিকেশন (SPA) — Vanilla HTML5, Modern JavaScript (ES6+ Fetch API), Tailwind CSS
* **ডেভেলপমেন্ট রানটাইম:** Uvicorn (Port 8000), VS Code Live Server (Port 5501)

---

## ২. ডাটাবেজ স্কিমা ও টেবিলসমূহ

ডাটাবেজে মোট **৮টি টেবিল** এবং **১টি রিলেশনাল ভিউ (View)** রয়েছে:

1. **`user`**: লগইন ও অথেনটিকেশন সংক্রান্ত মাস্টার টেবিল। ইমেইল, পাসওয়ার্ড এবং রোল (`admin`, `Faculty`, `Student`) ধারণ করে।
2. **`faculty`**: শিক্ষকদের প্রোফাইল তথ্য (নাম, পদবী, ডিপার্টমেন্ট, অফিস আওয়ার)। এটি `user` টেবিলের সাথে 1:1 ফরেন কি দিয়ে যুক্ত।
3. **`faculty_research_areas`**: একজন শিক্ষকের একাধিক রিসার্চ এরিয়া থাকতে পারে (1-to-Many)। কম্পোজিট প্রাইমারি কি: `(faculty_id, research_area)`।
4. **`student`**: শিক্ষার্থীদের ডাটা (নাম, ডিপার্টমেন্ট, সিজিপিএ, সেমিস্টার, গিটহাব ও সিভি লিংক)।
5. **`student_skills`**: শিক্ষার্থীদের স্কিলসমূহ (কম্পোজিট কি: `student_id, skill`)।
6. **`research_project`**: শিক্ষকদের প্রকাশিত গবেষণা প্রজেক্ট। এতে টাইটেল, ডেসক্রিপশন, রিকোয়ার্ড স্কিল, স্ট্যাটাস ও মোট আবেদনকারী কাউন্ট থাকে।
7. **`application`**: কোন প্রজেক্টে কোন শিক্ষার্থী আবেদন করেছে তার হিসাব। কম্পোজিট কি: `(project_id, application_id)`।
8. **`notification`**: সিস্টেমের যাবতীয় অনুমোদন ও রিকোয়েস্ট কিউ (JSON পে-লোড সহ)।
9. **`active_projects_view` (SQL View)**: শুধুমাত্র একটিভ প্রজেক্টগুলোর আইডি ও টাইটেল দ্রুত কুয়েরি করার জন্য তৈরি করা ডাটাবেজ ভিউ।

---

## ৩. ফাইলের পরিচিতি ও কাজের বিবরণ

* **`NiazBackend.py`**: FastAPI মেইন অ্যাপ। এতে CORS মিডলওয়্যার কনফিগারেশন এবং Auth, Admin, Faculty, Statistics ও Notification এর মোট ২২+ টি এপিআই এন্ডপয়েন্ট রয়েছে।
* **`crud.py`**: অ্যাপ্লিকেশনের মূল বিজনেস লজিক। সরাসরি কাঁচা এসকিউএল কুয়েরি (`db.execute(text(...))`), পাসওয়ার্ড চেক, টু-ফেজ অনুমোদন লজিক ও ডাটা প্রসেসিং এখানে লিখিত।
* **`schemas.py`**: Pydantic স্কিমা। লগইন, সাইন-আপ, আপডেট রিকোয়েস্ট ও এপিআই রেসপন্সের ডাটা টাইপ নিশ্চিত করে।
* **`models.py`**: SQLAlchemy মডেল যা ডাটাবেজ টেবিলের রূপরেখা তৈরি করে।
* **`database.py`**: MySQL ডাটাবেজ কানেকশন স্ট্রিং (`DATABASE_URL`), ইঞ্জিন এবং ডিপেন্ডেন্সি ইনজেকশন `get_db()` ফাংশন তৈরি করে।
* **`frontend.html`**: পিওর ফ্রন্টএন্ড সিঙ্গেল পেজ অ্যাপ্লিকেশন। এতে Tailwind CSS দিয়ে সুন্দর ডিজাইন, বিভিন্ন মডাল, লাইভ টোস্ট, ও ট্যাব সুইচিং কোড রয়েছে।
* **`research_management_db (5).sql`**: মূল ডাটাবেজ ডাম্প ফাইল (৩০ জন ফ্যাকাল্টি, ৩০ জন স্টুডেন্ট, ৫ জন অ্যাডমিন, ৪০ টি প্রজেক্ট)।
